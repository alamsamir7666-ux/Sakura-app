name: Build Flutter APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  analyze-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Run static analysis
        run: flutter analyze --no-fatal-infos || true

      - name: Run tests
        run: flutter test --reporter expanded || true

  build-apk:
    needs: analyze-and-test
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK (debug)
        env:
          CLERK_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY || 'pk_test_placeholder' }}
          API_URL: ${{ secrets.API_BASE_URL || 'https://envy-enhance-fixed5.vercel.app' }}
        run: |
          flutter build apk --debug \
            --dart-define=CLERK_PUBLISHABLE_KEY=$CLERK_KEY \
            --dart-define=API_BASE_URL=$API_URL

      - name: Build APK (release)
        env:
          CLERK_KEY: ${{ secrets.CLERK_PUBLISHABLE_KEY || 'pk_test_placeholder' }}
          API_URL: ${{ secrets.API_BASE_URL || 'https://envy-enhance-fixed5.vercel.app' }}
        run: |
          flutter build apk --release \
            --dart-define=CLERK_PUBLISHABLE_KEY=$CLERK_KEY \
            --dart-define=API_BASE_URL=$API_URL \
            --dart-define=FLAVOR=production

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: sakura-beauty-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 7

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: sakura-beauty-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30
